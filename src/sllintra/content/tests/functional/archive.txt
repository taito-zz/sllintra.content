Log in as Manager::

    >>> browser.login(TEST_USER_NAME, TEST_USER_PASSWORD)

Add Archive with archive_file only::

    >>> browser.open('/++add++sllintra.content.Archive')

    >>> import StringIO
    >>> dummy_file1 = StringIO.StringIO('ÄÖÅ text1')
    >>> browser.getControl(name='form.widgets.archive_file').mech_control.add_file(dummy_file1, content_type='text/plain', filename='äöå1.txt')
    >>> browser.getControl(name="form.buttons.save").click()

    >>> browser.url
    'http://nohost/plone/aoa1.txt/view'

    >>> print browser.bycss('.documentFirstHeading')
    --- 1 ---
    <h1 class="documentFirstHeading">&#228;&#246;&#229;1.txt</h1>

    >>> print browser.bycss('#form-widgets-archive_file')
    --- 1 ---
    <span id="form-widgets-archive_file" class="named-file-widget namedblobfile-field">
    <span>
    <img src="http://nohost/plone/txt.png" alt="Plain Text" title="&#228;&#246;&#229;1.txt"><a href="http://nohost/plone/aoa1.txt/@@view/++widget++form.widgets.archive_file/@@download/%C3%A4%C3%B6%C3%A51.txt">&#228;&#246;&#229;1.txt</a>
    <span class="discreet"> &#8212;
    Plain Text,
    1 KB
    </span>
    </span>
    </span>

    >>> browser.open('/aoa1.txt/@@view/++widget++form.widgets.archive_file/@@download/%C3%A4%C3%B6%C3%A51.txt')
    >>> print browser.contents
    ÄÖÅ text1

    >>> browser.open('/aoa1.txt/edit')

    >>> print browser.bycss('#form-widgets-title')
    --- 1 ---
    <input id="form-widgets-title" name="form.widgets.title" class="text-widget textline-field" value="&#228;&#246;&#229;1.txt" type="text">

Delete title and archive_file::

    >>> browser.getControl(name='form.widgets.title').value = ''
    >>> browser.getControl(name='form.widgets.archive_file.action').value = ['remove',]
    >>> browser.getControl(name="form.buttons.save").click()

    >>> browser.url
    'http://nohost/plone/aoa1.txt'

Complete only title::

    >>> browser.open('aoa1.txt/edit')
    >>> browser.getControl(name='form.widgets.title').value = 'Ötsikö1'
    >>> browser.getControl(name="form.buttons.save").click()

    >>> print browser.bycss('.documentFirstHeading')
    --- 1 ---
    <h1 class="documentFirstHeading">&#214;tsik&#246;1</h1>

Add new Archive without title and archive_file::

    >>> browser.open('/++add++sllintra.content.Archive')
    >>> browser.getControl(name="form.buttons.save").click()

    >>> browser.url
    'http://nohost/plone'

    >>> print browser.bycss('.info')
    --- 1 ---
    ...
    --- 2 ---
    <dl class="portalMessage info"><dt>Info</dt>
    <dd>You need to input title or add archive file to create Archive content type.</dd>
    </dl>

Complete only title::

    >>> browser.open('/++add++sllintra.content.Archive')
    >>> browser.getControl(name='form.widgets.title').value = 'Ötsikö2'
    >>> browser.getControl(name="form.buttons.save").click()

    >>> browser.url
    'http://nohost/plone/otsiko2/view'

    >>> print browser.bycss('.documentFirstHeading')
    --- 1 ---
    <h1 class="documentFirstHeading">&#214;tsik&#246;2</h1>

    >>> browser.open('/++add++sllintra.content.Archive')
    >>> browser.getControl(name='form.widgets.title').value = 'Ötsikö3'
    >>> browser.getControl(name="form.buttons.save").click()
    >>> browser.url
    'http://nohost/plone/otsiko3/view'

    # >>> browser.openBrowser()